# SCHEMA MIGRATION ANALYSIS REPORT

Date: 2026-04-22
Scope: Gateway, WhatsApp, Instagram, Slack, Notion, Facebook, TikTok, Identity services
Status: PENDING MIGRATIONS IDENTIFIED

## EXECUTIVE SUMMARY

### Current State:
- All 8 services have Prisma schemas with PostgreSQL datasources
- All 8 services have 1 initial migration (0_init) created
- CRITICAL: Schema definitions are INCONSISTENT across services
- CRITICAL: Gateway service schema is MISSING conversation message models
- WARNING: Identity service has DIFFERENT User model structure
- WARNING: N8NRateLimit model structure varies by service

## DETAILED FINDINGS:

### 1. GATEWAY SERVICE - CRITICAL ISSUE

Missing Models:
- ConversationMessage (exists in whatsapp, instagram, slack, facebook, tiktok, notion, identity)
- ConversationAIResponse (exists in whatsapp, instagram, slack, facebook, tiktok, notion, identity)
- User (exists in whatsapp, instagram, slack, facebook, tiktok, notion, identity)
- UserIdentity (exists in whatsapp, instagram, slack, facebook, tiktok, notion, identity)
- AIResponse (exists in whatsapp, instagram, identity)
- AIResponseChunk (exists in whatsapp, instagram, identity)

Current Migration 0_init Only Creates:
- Message
- IgMessage
- Conversation
- N8NRateLimit

Impact: BREAKING - The conversation room system cannot be used from Gateway.
Required Action: NEW MIGRATION NEEDED to add missing models.

### 2. WHATSAPP SERVICE - MOSTLY COMPLETE

Differences from Other Services:
- User model is MINIMAL (only id, aiEnabled) vs FULL in other services
- N8NRateLimit has maxCalls field (default 20)

Migration Status: 0_init includes all models OK

Issue: User model structure MISMATCH with Identity service

### 3. INSTAGRAM SERVICE - COMPLETE BUT DIFFERENT ENV

Database URL: Uses DATABASE_URL (non-standard, should be INSTAGRAM_DATABASE_URL)

Has Full Profile:
- User model MATCHES Identity service structure
- N8NRateLimit with maxCalls field
- All conversation models present

### 4. SLACK SERVICE - ARCHITECTURAL ISSUE

Schema Contains:
- ALL message types from all services (WaMessage, SlackMessage, NotionOperation, IgMessage, etc.)
- Full Identity models
- This violates microservice principle

Likely Explanation: This is a SHARED/CENTRAL DATABASE SCHEMA (template copied to all services)

### 5. NOTION SERVICE - SAME AS SLACK

IDENTICAL schema to Slack
Contains all message types and models from all services

### 6. FACEBOOK SERVICE - SAME PATTERN

IDENTICAL schema to Slack and Notion

### 7. TIKTOK SERVICE - SAME PATTERN

IDENTICAL schema to Slack, Notion, Facebook

### 8. IDENTITY SERVICE - SPECIALIZED

Has full User profile model with:
- realName, nicknames, nameTrustScore, nameSource, aiEnabled, aiEnabledAt, deletedAt

AIResponseChunk uses waMessageId instead of generic externalMessageId (WhatsApp-specific)

## KEY INCONSISTENCIES:

N8NRateLimit Structure Differs:

Gateway & Identity:
- userId with UNIQUE constraint
- callsToday field
- resetAt field

WhatsApp, Instagram, Slack, Notion, Facebook, TikTok:
- userId without unique constraint
- service field
- date field
- callCount field
- maxCalls field
- UNIQUE([userId, service, date])

INCOMPATIBLE - they have different purposes!

## ENVIRONMENT VARIABLE ISSUES:

Non-Standard Database URLs:
- Gateway: DATABASE_URL (should be GATEWAY_DATABASE_URL) - partially OK
- Identity: IDENTITY_DATABASE_URL - OK
- WhatsApp: WHATSAPP_DATABASE_URL - OK
- Slack: DATABASE_URL (should be SLACK_DATABASE_URL) - WRONG
- Notion: DATABASE_URL (should be NOTION_DATABASE_URL) - WRONG
- Facebook: DATABASE_URL (should be FACEBOOK_DATABASE_URL) - WRONG
- TikTok: DATABASE_URL (should be TIKTOK_DATABASE_URL) - WRONG
- Instagram: DATABASE_URL (should be INSTAGRAM_DATABASE_URL) - WRONG

5/8 services use generic DATABASE_URL causing conflicts!

## CRITICAL MIGRATIONS NEEDED:

1. Gateway Service URGENT:
   - Create migration: Add ConversationMessage, ConversationAIResponse, User, UserIdentity, AIResponse, AIResponseChunk
   
2. Slack/Notion/Facebook/TikTok VERIFY:
   - Check if 0_init actually created all 20+ declared models
   - May need new migrations for missing tables

3. Standardize N8NRateLimit:
   - Decide: Global per-user OR per-service-per-day
   - Recommend: Keep per-service-per-day (used by 6/8 services)

4. Fix Environment Variables:
   - Slack/Notion/Facebook/TikTok must use service-specific DATABASE_URL

## ACTUAL DATABASE STATE vs SCHEMA:

Gateway DB - Created by 0_init:
- Message table OK
- IgMessage table OK
- Conversation table OK
- N8NRateLimit table OK
- ConversationMessage - MISSING
- ConversationAIResponse - MISSING
- User - MISSING
- UserIdentity - MISSING
- AIResponse - MISSING
- AIResponseChunk - MISSING

WhatsApp DB - Created by 0_init:
- All 10 tables present OK

Instagram DB - Created by 0_init:
- All 10 tables present OK

Slack/Notion/Facebook/TikTok DB - Need to verify:
- Schema declares 20+ models
- Check what 0_init actually created
- Suspect many tables are missing

Identity DB - Created by 0_init:
- All User-related tables present OK
- AIResponse tables present OK

## RECOMMENDATIONS:

Priority 1 - FIX GATEWAY (URGENT):
- Create new migration to add missing conversation models
- Timeline: BLOCKS message flow - do immediately

Priority 2 - VERIFY SLACK/NOTION/FACEBOOK/TIKTOK:
- Check what tables actually exist
- Create missing migrations if needed

Priority 3 - STANDARDIZE N8NRateLimit:
- Gateway & Identity should match other services
- Use per-service-per-day approach

Priority 4 - FIX ENVIRONMENT VARIABLES:
- Update Slack/Notion/Facebook/TikTok to use service-specific DATABASE_URL

Priority 5 - CONSOLIDATE SCHEMAS:
- Each service should only include service-specific models + shared identity models
- Remove duplicated definitions

## ACTION ITEMS:

Immediate:
- Create Gateway migration for missing conversation models
- Verify Slack/Notion/Facebook/TikTok 0_init completeness
- Document which service owns which tables

Short-term:
- Fix environment variable naming
- Standardize N8NRateLimit approach
- Consolidate schema definitions

Time to Fix: 2-4 hours

